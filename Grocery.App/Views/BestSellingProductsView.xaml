<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
             xmlns:m="clr-namespace:Grocery.Core.Models;assembly=Grocery.Core"
             x:Class="Grocery.App.Views.BestSellingProductsView"
             x:DataType="vm:BestSellingProductsViewModel"
             Title="Meest verkochte producten">

    <Shell.TitleView>
        <Grid>
            <Label Text="Meest verkochte producten"
                   FontSize="Large"
                   HorizontalOptions="Center"
                   VerticalOptions="Center" />
        </Grid>
    </Shell.TitleView>

    <Border Stroke="#C49B33"
            StrokeThickness="4"
            Padding="10"
            Margin="10"
            HorizontalOptions="Center">
        <CollectionView ItemsSource="{Binding Products}"
                        Margin="20"
                        HorizontalOptions="Center">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
            </CollectionView.ItemsLayout>

            <!-- HEADER -->
            <CollectionView.Header>
                <Grid ColumnDefinitions="0.5*,*,*,*"
                      Padding="4,0">
                    <Label Text="#" Grid.Column="0" FontAttributes="Bold" />
                    <Label Text="Product" Grid.Column="1" FontAttributes="Bold" />
                    <Label Text="Verkocht" Grid.Column="2" HorizontalTextAlignment="End" FontAttributes="Bold" />
                    <Label Text="Voorraad" Grid.Column="3" HorizontalTextAlignment="End" FontAttributes="Bold" />
                </Grid>
            </CollectionView.Header>

            <!-- RIJ TEMPLATE -->
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="m:BestSellingProducts">
                    <Grid ColumnDefinitions="0.5*,*,*,*" Padding="4,0">
                        <Label Text="{Binding Ranking}" Grid.Column="0" />
                        <Label Text="{Binding Name}" Grid.Column="1" />
                        <Label Text="{Binding NrOfSells}" Grid.Column="2" HorizontalTextAlignment="End" />
                        <Label Text="{Binding Stock}" Grid.Column="3" HorizontalTextAlignment="End" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>

        </CollectionView>
    </Border>
</ContentPage>
